streamlit>=1.49
# ⚠️ pandas は固定する。上限が無いと Render が再デプロイのたびに最新を入れ、
# 手元と違うバージョンで動く。そうなると「遅い・数字が違う」の切り分けができない
# （2026-09-07 の性能調査で実際に、手元 3.0.5 に対して Render 側が不明だった）。
# 上げるときは手元で動かして確かめてから、ローカル版の requirements.txt と同時に直す
pandas==3.0.5
plotly>=5.20
cryptography>=42.0
Authlib>=1.3.2
httpx>=0.27
